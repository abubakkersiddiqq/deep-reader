langchain_community
langchain_classic 
langchain_text_splitters
langchain_chroma
langchain_huggingface (sentence-transformers - required to run in local)
PyPDF - (required to import the document_loader from the coummunity)
langchain_openai
dotenv

functions - rag.py 
 it's preivew - i'll update later
 1. load(file) - clean repeated words like name etc
   load the pdf and returns the document
 2. chunks(document)
   chunk the document using langchain_text_splitters
   returns -> doc_chunks
 3. vectorizer(doc_chunks)
   vectorize the chunks using langchain_chroma and langchain_huggingface
   returns ->vector db
 4. llm()
   setup for llm
   return llm 
 5. prompt()
   return prompt
   

 
Two main fuunction
/upload
def index_document(pdf_path) -> vectordb

# This runs on /ask  
def get_answer(question) -> str

Chroma load from persist_directory langchain

if app.state.vector_db is None:
    return {"error": "No document uploaded yet"}